import tkinter as tk
import time

'''
class Timer(tk.Frame):



if __name__ == '__main__':
    winForm = tk.Tk()
    sw = Timer()
    sw.pack(side=tk.TOP)
    tk.Button(winForm, text='开始', command=sw.Start).pack(side=tk.LEFT)
    tk.Button(winForm, text='暂停', command=sw.Stop).pack(side=tk.LEFT)
    tk.Button(winForm, text='重置', command=sw.Reset).pack(side=tk.LEFT)
    tk.Button(winForm, text='退出', command=sw.quit).pack(side=tk.LEFT)
    winForm.mainloop()
'''